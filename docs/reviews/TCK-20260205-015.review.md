# Doc Review: TCK-20260205-015

**Date:** 2026-03-08  
**Reviewer:** Amp (Doc-to-Code Audit Agent)

## Scope

- Validate ticket `docs/tickets/TCK-20260205-015.md` against current repo code.
- Resolve high-impact open item from reconciliation (`camera skip handler wiring`).

## Reality Summary

- Guest auth/session support is present and functional in `src/frontend/src/store/authStore.ts`.
- Voice prompt plumbing is present in `src/frontend/src/hooks/useVoicePrompt.ts`, `src/frontend/src/components/ui/VoiceButton.tsx`, and tutorial integration.
- Original dashboard-specific claims in the ticket are partially stale after dashboard redesign.
- Camera fallback UI existed in tutorial, but integration gap remained in the page wiring.

## Implemented Resolution (This Pass)

1. `src/frontend/src/pages/AlphabetGame.tsx`
   - Added `handleSkipCameraTutorial` and passed it to `GameTutorial` as `onSkipCamera`.
   - Updated `startGame()` to respect preselected touch/mouse mode and skip camera permission request.
   - Updated start button label logic to show `Play with Mouse/Touch` when `useMouseMode` is selected.

2. `src/frontend/src/pages/__tests__/AlphabetGame.cameraSkip.test.tsx`
   - Added regression tests for:
   - tutorial receives `onSkipCamera` handler
   - camera permission is not requested when start follows skip-camera choice

3. `docs/tickets/TCK-20260205-015.md`
   - Added revalidation section and decision log.
   - Updated stale path reference to `src/frontend/src/pages/AlphabetGame.tsx`.
   - Updated testing checklist with verified vs pending items.

## Decisions

1. Preserve current dashboard redesign as source of truth, rather than forcing historical copy claims.
2. Preserve current guest CTA wording (`Try as Guest`) unless product requests copy change.
3. Keep unresolved dashboard contrast/zero-state checks as explicit follow-up validation work.

## Remaining Follow-Up

- Perform current-dashboard WCAG AA contrast verification with measured evidence.
- Run fresh mobile/tablet/desktop viewport pass for the ticket checklist.
