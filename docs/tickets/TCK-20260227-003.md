# TCK-20260227-003 :: Add Progress Tracking to All Games

Ticket Stamp: STAMP-20260227T000200Z-audit-progress

**Type**: QUALITY_FIX  
**Owner**: Unassigned  
**Created**: 2026-02-27  
**Status**: OPEN  
**Priority**: P0 (CRITICAL)  
**Severity**: Core value proposition broken

---

## Problem

**95% of games (37/39) don't save learning progress.**

Children complete games but their progress isn't tracked, breaking:
- Parent dashboard insights
- Learning analytics
- Achievement system
- Progress-based recommendations

---

## Scope Contract

**In-scope**:
- Add progressQueue integration to all 39 games
- Save completion status
- Save scores
- Handle offline queuing

**Out-of-scope**:
- Backend progress API (already exists)
- Dashboard UI changes

**Behavior change allowed**: YES (progress will now be saved)

---

## Technical Design

### Pattern to Add to Each Game

```typescript
import { progressQueue } from '../services/progressQueue';
import { useProgressStore } from '../store';

const { currentProfile } = useProgressStore();

// On game complete:
const handleGameComplete = useCallback(async (score: number, completed: boolean) => {
  try {
    await progressQueue.add({
      profileId: currentProfile.id,
      gameId: 'game-id',
      score,
      completed,
      metadata: {
        // Game-specific metadata
        level: currentLevel,
        timeSpent: timeSpent,
      },
    });
  } catch (error) {
    console.error('Failed to save progress:', error);
    // Queue will retry automatically
  }
}, [currentProfile, currentLevel, timeSpent]);
```

---

## Plan

- [ ] Create progress tracking template
- [ ] Add to 39 games (8-10 per day)
- [ ] Test progress saves correctly
- [ ] Verify appears in dashboard
- [ ] Test offline queuing

---

## Acceptance Criteria

- [ ] All 39 games save progress on completion
- [ ] Progress appears in parent dashboard
- [ ] Offline queuing works
- [ ] Retry logic functions
- [ ] No duplicate entries
- [ ] Scores are accurate

---

## Dependencies

- Progress queue service (exists)
- Progress API (exists)
- Profile store (exists)

---

**Estimated Effort**: ~20 hours (2.5 days)
