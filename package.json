{
  "scripts": {
    "check:db-migration": "bash ./scripts/ci_db_migration_check.sh",
    "check:mandatory": "pnpm run -s check:db-migration && pnpm -C src/frontend run -s type-check && pnpm -C src/frontend run -s test && (cd src/backend && uv run pytest -q)"
  },
  "dependencies": {
    "@mediapipe/camera_utils": "^0.3.1675466862",
    "@mediapipe/hands": "^0.4.1675469240",
    "matter-js": "^0.20.0",
    "playwright": "^1.58.2",
    "puppeteer": "^24.37.5"
  },
  "packageManager": "pnpm@10.30.1",
  "devDependencies": {
    "fast-check": "^4.5.3"
  }
}
