# CI entrypoint notice
#
# GitHub Actions are active in this repository via `.github/workflows/deploy.yml`.
# This file is intentionally a no-op placeholder to avoid duplicate workflow definitions.
#
# Local quality gates in `.githooks/` and `scripts/*` remain mandatory for contributors,
# and should mirror/strengthen remote checks where possible.
