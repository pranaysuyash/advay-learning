import { test } from '@playwright/test';

// Playwright tests pending implementation
test.fixme('Mobile layout: mascot does not overlap CTA (screenshot diff)');

// Example flow to implement later:
// test('Try Demo flow does not request camera permission', async ({ page }) => {
//   await page.goto('/')
//   await page.click('text=Try Demo')
//   // assert no permission dialog and demo content visible
// })
